package march.twozero.twentieth;

import java.util.Arrays;
import java.util.stream.*;

public class ArrayUtility2 {
	public static int[] concat(int[] s1, int[] s2) {	//s1과 s2를 연결한 새로운 배열 리턴
		int[] s3 = IntStream.concat(Arrays.stream(s1), Arrays.stream(s2)).toArray();
		for(int i = 0; i < s3.length; i++) {
			System.out.print(s3[i] + ", ");
		}
		return s3;
	}
	
	//public static int[] remove(int[] s1, int[] s2) {	//s1에서 s2배열의 숫자를 모두 삭제한 새로운 배열 리턴
		
}